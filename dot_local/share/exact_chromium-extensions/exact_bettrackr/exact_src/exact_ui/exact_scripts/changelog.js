export async function init(){try{const n=await fetch(chrome.runtime.getURL("changelog.json")).then((n=>n.json())),a=$("#changelog");n.forEach((n=>{const e=$(`\n          <div class="version-header">\n            <span class="date">${n.date}</span> -  v${n.version}\n          </div>\n        `);a.append(e);const c=$('<ul class=""></ul>');n.changes.forEach((n=>{c.append(`<li class="small p-1">${n}</li>`)})),a.append(c)}))}catch(n){$("#changelog").text("Unable to load changelog.")}}