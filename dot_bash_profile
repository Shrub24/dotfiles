# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
    . ~/.bashrc
fi

# User specific environment and startup programs

. "$HOME/.local/bin/env"
. "$HOME/.cargo/env"

source /home/saurabhj/.config/broot/launcher/bash/br
